import { FeaturePlaceholder } from "@/components/feature-placeholder";

export default function SearchPage() {
  return (
    <FeaturePlaceholder eyebrow="Find a bottle" title="Search">
      Search will cover producer, wine, vintage, country, region, appellation
      and grapes, with simple shelf and vintage filters.
    </FeaturePlaceholder>
  );
}
