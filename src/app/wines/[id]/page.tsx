import { FeaturePlaceholder } from "@/components/feature-placeholder";

export default function WineDetailPage() {
  return (
    <FeaturePlaceholder eyebrow="Cellar entry" title="Wine Detail">
      This personal wine entry will hold the label, bottle balance, drinking
      window, notes and drinking history.
    </FeaturePlaceholder>
  );
}
