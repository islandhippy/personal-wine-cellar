import { FeaturePlaceholder } from "@/components/feature-placeholder";

export default function AddWinePage() {
  return (
    <FeaturePlaceholder eyebrow="New cellar entry" title="Add Wine">
      The next stage will add the quick label-photo and wine-details workflow,
      defaulting to one standard bottle.
    </FeaturePlaceholder>
  );
}
